# Pinned versions / sources for setup-mac. Single source of truth.
# Determinate Nix installer (robust macOS APFS handling, clean uninstall).
NIX_INSTALLER_URL="https://install.determinate.systems/nix"
# Packages installed into the default nix profile.
NIX_PACKAGES=(
  "nixpkgs#nmap"
)
