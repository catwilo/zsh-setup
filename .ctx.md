# zsh-setup — context
## status: IN PROGRESS (B-unification)

## confirmed
- install.sh: POSIX sh entrypoint; flags --mpd --mac --dry-run
- optional/mpd/install.sh: setup_mpd() — Termux+Debian
- optional/mac/install.sh: setup_mac() + fix_clipboard() — Darwin-guarded
- lib/plugins.sh: copy from dotconfig bundle → git clone fallback
- lib/links.sh: NOW uses internal dotfiles/ path (no dotconfig dep) ✓
- termux-setup: ABSORBED + DELETED ✓
- dotfiles/: migrated from dotconfig (B2 ✓); install.sh + all dotfiles present

## structure
- install.sh
- lib/core.sh detect.sh pkg.sh plugins.sh links.sh
- dotfiles/            ← NEW: single source of dotfiles (absorbed from dotconfig)
- optional/mpd/
- optional/mac/
- packages/
- configs/

## arch (current)
- links.sh: DOTFILES=$(dirname BASH_SOURCE[0])/../dotfiles; no external dep ✓
- plugins.sh: copy from dotfiles/.addons-zsh/ bundle → git fallback
  NOTE: plugins.sh still references dotconfig path — needs update (B3 pending verify)

## pending
- lipso + widget: add to dotfiles
  - lipso(): 1-arg→eval "$1", multi-arg→"$@"; ambos pipean a clipso
  - _wrap_clipso widget: envuelve BUFFER en { ...; } 2>&1 | clipso, bind Alt+g (^[g)
  - target: dotfiles/.addons-zsh/aliass/ o dotfiles/.zshrc
- B3-verify: update plugins.sh to read bundle from dotfiles/.addons-zsh/ (not dotconfig)
- B5: dry-run --dry-run Termux → d0 → d1
- commit + push after B5 green
- dotconfig deletion blocked on B5 all-green

## last-known-good
- lib/links.sh: internal dotfiles/ path, syntax ok, not yet committed
- dotfiles/: fully populated from dotconfig ✓
