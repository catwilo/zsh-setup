# zsh-setup — context
## status: STABLE
## confirmed
- setup.sh: POSIX sh entrypoint; flags --mpd --mac --dry-run
- optional/mpd/install.sh: setup_mpd() — Termux+Debian
- optional/mac/install.sh: setup_mac() + fix_clipboard() — Darwin-guarded (absorbed setup-mac)
  - setup_mac: Nix + deps + PATH via lib/nix.sh lib/deps.sh lib/path.sh
  - fix_clipboard: bootout io.clipd.agent + pkill -9 nc/pbcopy + killall pboard
- bash-array syntax in optional/mac/ is Darwin-only; sourced inside Darwin guard — POSIX sh never parses it
## structure
- setup.sh            entrypoint
- lib/core.sh detect.sh pkg.sh plugins.sh links.sh
- optional/mpd/       MPD setup
- optional/mac/       macOS bootstrap (absorbed from setup-mac)
- packages/           per-platform package lists
- configs/            dotfile sources
## pending
- validate: run sh setup.sh --mac on d1 Mac end-to-end
- validate: zsh-setup client install on d1 (T2 in macro ctx)
